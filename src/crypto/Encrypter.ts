export function encryptPhrase(phrase: string) {
  /// TODO : To be expanded further by picking the stored password and encrypt the phrase
  /// TODO : A sync call should be done on deciding best approach regarding this step
  return phrase;
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function decryptPhrase(encryptedPhrase: string, password: string) {
  /// TODO : To be expanded further by picking the stored password and decrypt the encryptedPhrase
  return encryptedPhrase;
}
